import { getCurrencySymbol } from "./numbers.utils";
import { mask }  from "./strings.utils"

/** Modular export para mas lighweight ang library */
export const currencySymbol = getCurrencySymbol
export const maskString = mask